#!/bin/bash

# Keep this file minimal — shared vars/functions go here.
# YunoHost's helpers already export $app, $domain, $port,
# $install_dir, $data_dir, etc.

# Nothing extra needed for a simple binary install.
